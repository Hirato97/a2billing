{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "#/a2billing-go-api/common/schema/update-card-status.json",
  "title": "update-card-status",
  "type": "object",
  "description": "update-card-status",
  "required": ["status"],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["active", "expired","cancelled","suspended"]
    }
  }
}
